import { fetchAuthSession, signIn } from 'aws-amplify/auth';
import { Amplify } from 'aws-amplify';
import { generateClient } from 'aws-amplify/api';

export {
  fetchAuthSession, signIn, Amplify, generateClient,
};
